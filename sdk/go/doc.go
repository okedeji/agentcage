// Package agentcage is the Go SDK for agent developers. It provides
// a client for submitting findings from inside a cage, reading
// directives from the orchestrator, and requesting agent holds when
// the agent needs a human decision before proceeding.
package agentcage
