// Package proofs embeds the default proof-of-concept validation
// rules shipped with agentcage. Each YAML file defines how a
// validator cage confirms a candidate finding: what payload to send,
// what response to expect, and what safety constraints apply.
// Operators can add custom rules alongside these via the
// agentcage proof command.
package proofs
