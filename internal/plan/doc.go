// Package plan owns the YAML-based assessment plan file that
// operators commit alongside their agent. The plan describes what
// to attack, how long, and with what guidance. CLI flags override
// individual plan values so a single plan works across environments.
package plan
